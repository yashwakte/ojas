{
  "name": "ojas-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for Ojas, run against the API and Angular dev servers on localhost.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "mongodb": "^7.5.0"
  }
}
